{
  "name": "calnetstudy",
  "version": "1.0.0",
  "private": true,
  "description": "Study notes, all subjects, one place.",
  "scripts": {
    "build": "node build.js",
    "serve": "node serve.js",
    "dev": "node build.js && node serve.js"
  },
  "devDependencies": {
    "marked": "^12.0.2"
  }
}
